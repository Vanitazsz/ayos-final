import { z } from "zod";
import { paginationSchema } from "../utils/pagination.js";
const uuid=z.string().uuid(); export const uuidParam=z.object({id:uuid});
export const conversationCreate=z.object({participantIds:z.array(uuid).min(1).max(10),requestId:uuid.optional(),bookingId:uuid.optional()});
export const messageCreate=z.object({text:z.string().trim().max(5000).optional(),attachmentIds:z.array(uuid).max(10).default([])}).refine(v=>Boolean(v.text)||v.attachmentIds.length>0,"A message or attachment is required");
export const conversationList=paginationSchema.extend({search:z.string().trim().max(100).optional()}); export const readMessage=z.object({messageId:uuid.optional()});
export const reviewList=paginationSchema.extend({providerId:uuid.optional(),rating:z.coerce.number().int().min(1).max(5).optional(),status:z.enum(["PUBLISHED","HIDDEN","FLAGGED"]).optional(),search:z.string().trim().max(100).optional()});
export const reviewCreate=z.object({bookingId:uuid,rating:z.number().int().min(1).max(5),comment:z.string().trim().max(5000).optional(),recommend:z.boolean().default(true),photoUploadIds:z.array(uuid).max(6).default([])}); export const reviewUpdate=reviewCreate.omit({bookingId:true}).partial(); export const helpful=z.object({helpful:z.boolean()}); export const reviewReport=z.object({reason:z.string().trim().min(1).max(200),details:z.string().trim().max(2000).optional()});
export const notificationList=paginationSchema.extend({unread:z.coerce.boolean().optional(),type:z.string().max(64).optional()});
export const ticketList=paginationSchema.extend({status:z.string().max(30).optional(),search:z.string().trim().max(100).optional()}); export const ticketCreate=z.object({subject:z.string().trim().min(3).max(250),category:z.string().trim().min(1).max(100),priority:z.enum(["LOW","MEDIUM","HIGH"]).default("MEDIUM"),message:z.string().trim().min(1).max(5000),bookingId:uuid.optional(),attachmentIds:z.array(uuid).max(10).default([])}); export const ticketMessage=z.object({message:z.string().trim().min(1).max(5000),attachmentIds:z.array(uuid).max(10).default([])});
export const paymentCreate=z.object({bookingId:uuid,method:z.string().trim().min(1).max(32),providerToken:z.string().max(255).optional(),idempotencyKey:z.string().trim().min(8).max(128)}); export const refundCreate=z.object({amount:z.number().int().positive().optional(),reason:z.string().trim().min(3).max(1000)});
export const reviewReply=z.object({message:z.string().trim().min(1).max(2000)});export const reviewModerate=z.object({status:z.enum(["PUBLISHED","HIDDEN","FLAGGED"]),reason:z.string().max(1000).optional()});export const ticketUpdate=z.object({assigneeId:uuid.nullable().optional(),priority:z.enum(["LOW","MEDIUM","HIGH"]).optional(),status:z.enum(["OPEN","PENDING","ESCALATED","RESOLVED","CLOSED"]).optional()});export const resolution=z.object({resolution:z.string().trim().max(2000).optional()});
