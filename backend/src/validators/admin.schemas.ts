import{z}from"zod";import{paginationSchema}from"../utils/pagination.js";export const adminList=paginationSchema.extend({search:z.string().trim().max(100).optional(),status:z.string().max(40).optional()});export const statusChange=z.object({status:z.enum(["ACTIVE","PENDING","SUSPENDED","DISABLED"]),reason:z.string().max(1000).optional()});export const workerDecision=z.object({remarks:z.string().trim().max(2000).optional()});export const categoryBody=z.object({name:z.string().trim().min(1).max(120),slug:z.string().trim().regex(/^[a-z0-9-]+$/).optional(),icon:z.string().max(100).optional(),color:z.string().max(20).optional(),status:z.enum(["ACTIVE","INACTIVE"]).default("ACTIVE")});export const serviceBody=z.object({categoryId:z.string().uuid(),name:z.string().trim().min(1).max(150),slug:z.string().trim().regex(/^[a-z0-9-]+$/).optional(),description:z.string().max(2000).optional(),basePrice:z.number().int().nonnegative().optional(),durationMinutes:z.number().int().positive().optional(),status:z.enum(["ACTIVE","INACTIVE"]).default("ACTIVE")});export const settingsBody=z.record(z.string().min(1).max(120),z.unknown());export const campaignBody=z.object({title:z.string().trim().min(1).max(200),audience:z.enum(["ALL_USERS","WORKERS_ONLY","CUSTOMERS_ONLY","INACTIVE_USERS"]),channel:z.enum(["IN_APP","EMAIL","SMS","PUSH"]),message:z.string().trim().min(1).max(5000),status:z.enum(["DRAFT","SCHEDULED"]).default("DRAFT"),scheduledAt:z.coerce.date().optional()});export const reportBody=z.object({type:z.string().trim().min(1).max(100),format:z.enum(["json","csv"]).default("json"),from:z.coerce.date().optional(),to:z.coerce.date().optional()}).refine(v=>!v.from||!v.to||v.to>v.from,{path:["to"],message:"to must be after from"});
