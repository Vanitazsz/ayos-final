import { Router } from "express";
import { adminController as a } from "../controllers/admin.controller.js";
import { authenticate, requirePermission } from "../middleware/auth.js";
import { validate } from "../middleware/validate.js";
import { adminList,campaignBody,categoryBody,reportBody,serviceBody,settingsBody,statusChange,workerDecision } from "../validators/admin.schemas.js";
import { uuidParam } from "../validators/domain.schemas.js";
export const adminRouter=Router();adminRouter.use(authenticate);
adminRouter.get("/dashboard",requirePermission("analytics:view"),a.dashboard);adminRouter.get("/analytics",requirePermission("analytics:view"),a.dashboard);
adminRouter.get("/users",requirePermission("users:view"),validate({query:adminList}),a.users);adminRouter.get("/users/:id",requirePermission("users:view"),validate({params:uuidParam}),a.user);adminRouter.post("/users/:id/status",requirePermission("users:manage"),validate({params:uuidParam,body:statusChange}),a.userStatus);
adminRouter.get("/workers",requirePermission("workers:view"),validate({query:adminList}),a.workers);adminRouter.get("/workers/:id",requirePermission("workers:view"),validate({params:uuidParam}),a.worker);adminRouter.post("/workers/:id/approve",requirePermission("workers:verify"),validate({params:uuidParam,body:workerDecision}),a.approve);adminRouter.post("/workers/:id/reject",requirePermission("workers:verify"),validate({params:uuidParam,body:workerDecision.refine(v=>Boolean(v.remarks),"Remarks required")}),a.reject);adminRouter.post("/workers/:id/request-documents",requirePermission("workers:verify"),validate({params:uuidParam,body:workerDecision}),a.documents);
adminRouter.get("/categories",requirePermission("catalog:view"),validate({query:adminList}),a.categories);adminRouter.post("/categories",requirePermission("catalog:manage"),validate({body:categoryBody}),a.createCategory);adminRouter.patch("/categories/:id",requirePermission("catalog:manage"),validate({params:uuidParam,body:categoryBody.partial()}),a.updateCategory);
adminRouter.get("/services",requirePermission("catalog:view"),validate({query:adminList}),a.services);adminRouter.post("/services",requirePermission("catalog:manage"),validate({body:serviceBody}),a.createService);adminRouter.patch("/services/:id",requirePermission("catalog:manage"),validate({params:uuidParam,body:serviceBody.partial()}),a.updateService);
adminRouter.get("/bookings",requirePermission("bookings:view"),validate({query:adminList}),a.bookings);adminRouter.get("/payments",requirePermission("payments:view"),validate({query:adminList}),a.payments);adminRouter.get("/payments/summary",requirePermission("payments:view"),a.dashboard);
adminRouter.get("/settings",requirePermission("settings:view"),a.settings);adminRouter.patch("/settings",requirePermission("settings:manage"),validate({body:settingsBody}),a.updateSettings);adminRouter.get("/audit-logs",requirePermission("audit:view"),validate({query:adminList}),a.audit);adminRouter.get("/trash",requirePermission("recovery:view"),validate({query:adminList}),a.trash);adminRouter.post("/trash/:id/restore",requirePermission("recovery:manage"),validate({params:uuidParam}),a.restore);
adminRouter.get("/campaigns",requirePermission("campaigns:view"),validate({query:adminList}),a.campaigns);adminRouter.post("/campaigns",requirePermission("campaigns:manage"),validate({body:campaignBody}),a.createCampaign);adminRouter.get("/campaigns/:id",requirePermission("campaigns:view"),validate({params:uuidParam}),a.campaign);adminRouter.patch("/campaigns/:id",requirePermission("campaigns:manage"),validate({params:uuidParam,body:campaignBody.partial()}),a.updateCampaign);adminRouter.post("/campaigns/:id/send",requirePermission("campaigns:send"),validate({params:uuidParam}),a.sendCampaign);
adminRouter.get("/reports",requirePermission("reports:view"),validate({query:adminList}),a.reports);adminRouter.post("/reports",requirePermission("reports:manage"),validate({body:reportBody}),a.generateReport);adminRouter.get("/reports/:id/download",requirePermission("reports:view"),validate({params:uuidParam}),a.reportDownload);
