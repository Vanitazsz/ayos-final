import type{Request,Response}from"express";import{createReadStream}from"node:fs";import{requireUploadFile}from"../middleware/upload.js";import{uploadService}from"../services/upload.service.js";import{sendNoContent,sendSuccess}from"../utils/response.js";
export const uploadController={create:async(r:Request,s:Response)=>{const u=await uploadService.create(r.auth!.userId,String(r.body.purpose??""),requireUploadFile(r.file),r.body.visibility==="PUBLIC"?"PUBLIC":"PRIVATE");return sendSuccess(s,{id:u.id,url:u.visibility==="PUBLIC"?`/api/v1/uploads/${u.id}`:undefined,mimeType:u.mimeType,size:u.sizeBytes},"File uploaded",201)},get:async(r:Request,s:Response)=>{const{upload,path}=await uploadService.get(r.auth?.userId,r.auth?.roles??[],String(r.params.id));s.type(upload.mimeType).set("Content-Length",String(upload.sizeBytes)).set("Content-Disposition",`inline; filename*=UTF-8''${encodeURIComponent(upload.originalName)}`);createReadStream(path).on("error",e=>s.destroy(e)).pipe(s)},remove:async(r:Request,s:Response)=>{await uploadService.remove(r.auth!.userId,r.auth!.roles,String(r.params.id));return sendNoContent(s)}};
