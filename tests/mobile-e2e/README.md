# Mobile E2E tests

`public-entry.spec.ts` executes the Expo web entry, role-specific registration-route, validation, and responsive-layout checks. Native device automation and authenticated lifecycle fixtures remain blocked until the supported Android/iOS matrix and acceptance environment are supplied.
